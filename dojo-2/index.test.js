const assert = require('assert');

describe('Dojo 02', () => {
  it('should make string interpolation work', () => {
    // assert.equal(getMiddle("test"), "es");
  });

  it('should make regex work', () => {
    // assert.equal(getMiddle("test"), "es");
  });

  it('should match literals', () => {
    // assert.equal(getMiddle("test"), "es");
  });
});
